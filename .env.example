# Copy to .env and replace the example secret before enabling write endpoints.
APP_VERSION=1.0.0
AUTH_API_KEYS=replace-with-a-long-random-secret
# AI_API_KEY is optional. Leave empty to disable the chat integration.
AI_API_KEY=
LOG_LEVEL=info
HTTP_PORT=80
HTTPS_PORT=443
